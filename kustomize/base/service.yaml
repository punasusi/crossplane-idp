---

apiVersion: v1
kind: Service
metadata:
  name: crossplane-idp
  labels:
    app: crossplane-idp
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: crossplane-idp
